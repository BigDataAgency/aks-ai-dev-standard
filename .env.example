# AKS AI Dev Standard public-repo local defaults
# Copy to .env for local experiments only. Do not commit real credentials.
# Legacy BDA_* env vars remain supported permanently as fallback aliases.

AKS_STANDARD_MODE=local
INNOHUB_INGEST_URL=
INNOHUB_INGEST_TOKEN=
INNOHUB_TENANT_ID=
